# Setup
\
\
* This assumes that Node, NPM/NPX, and Astro have already been installed, along with the Git repo being cloned into a local directory and connected properly to sync changes.*
\
**This will only work for development, not hosting as this will require a webserver to host the final output and a few other tweaks and for security.**
\
\
**Install the following:**
- 
\
\
\
* These are an example of what can be done with a Debian/Ubuntu type system.*
apt install git node npm
git clone https://github.com/GameTours/jtyoung-me
cd jtyoung-me
npm install
npm run build